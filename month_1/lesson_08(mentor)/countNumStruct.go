package main

func main() {
	var arr = []int{1, 2, 3, 3, 54, 65, 76, 4, 1, 2, 1}
	countNumber(arr)
}

func countNumber(arr []int) {

	// var counter int
	type valueCount struct {
		value int
		count int
	}

	for _, value := range arr {
		if valueCount.count == value {

		}
	}

	// for num, count := range CoContainer {
	// 	fmt.Printf("%d repeated: %d times\n", num, count)
	// }

}
